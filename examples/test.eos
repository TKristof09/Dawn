fun testFn(x: i64, y: i64)
{
    if x == 0
    {
        print(2000);
    }
    else
    {
        print(x);
        print(y);
        x = x - 1;
        testFn(x, y);
    }
};

let y:i64 = 5;
let x:i64 = 69;
testFn(y, x);
