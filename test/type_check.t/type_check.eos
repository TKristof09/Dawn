fun testFn(x: int, y: int) -> bool
{
    if(x == 0)
    {
        print(2000);
    }
    else
    {
        print(x);
        print(y);
        x = x - 1;
        testFn(x, y);
    }
}

let y:bool = 5;
let x:int = 69;
testFn(y, x, y);
